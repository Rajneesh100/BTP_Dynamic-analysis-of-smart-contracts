/*
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#5GB#&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@Y!5555555PGB##&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@Y?GPGGGGGPPP5555PPGGBB#&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@J?PPPPPPGGGGGGGPPPPPPPPPPPGGBB##&&@@@@@@@@@@@@@@@@@@@@@@&&&@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&#BGJ!^?PPPPPPPPGGGGGGPPPPPPPPPPPPPPPPPGGGGGBB###&&&@@@&&###BBBBBB###&&&@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@&&#BGPYJ7!!!7??J7JPPGGGGGGGGGGGGGPPGGGGGGGPPPPPPPPPPPPPPPPPPPPPPGPY?77??JJY55PPGBB#&&@@@@@@@@@@@
@@@@@@@@@@@@@@@@&#BP5J?7!~~^^^^^^~!7JYPG?YGGGGGGGGGGGGGGGGGGGGGGGGGGGGGGGGGPPPPPPPPPPPP55J7~^:.....::^~!?Y5GB#&@@@@@@@@@
@@@@@@@@@@@@@BY7!~^~^~~~!!!77~^^^!55PGGBJYGGGGGGGGGGGGGGGGGP~^^^YBGGGGGGGGGGGGGGGGPPPPPP55YJ7!!!!!~^::.  .~J5B#@@@@@@@@@
@@@@@@@@@@@@Y^^!777??JJJJJJYY~^^^!G5PGGBJYGGGGG5~:::!55555PP.   ?P555PGGG55555555PGGGPPPP5JJJJYYYYJJJ??7~^ ^5G#&@@@@@@@@
@@@@@@@@@@@P~~7??JJYYYYYYYYYY~^^^7GPPGBBJYGGGG5.          JG:     ...:5P^...  ...^5GPPP55YY5PPPPPPPP55YYYY?!5##&@@@@@@@@
@@@@@@@@@@#!???7JJJJJYYJJ7!~~^^^^^!!7J5BJ5GGGGP!.   ~????7PG:   !7    Y5.   7!   .5GPPPPPPPPPPPPPPPPPPP5PPP5?#&@@@@@@@@@
@@@@@@@@&@?~7?7?JJJJJ?7~^^^::^^^^^:::^^~!YGGGGGB^   7B!^^^5G:   5G.   55.   77   .5PPPPPPPPPPPPPPPGGGGGPPGPPJY@@@@@@@@@@
@@@@@@&#&5^~77?JJJJJ!^^^^:~!?JYYYYY?7^^^^^75GGGB^   !G:   YG:   YP.   55.   .::::^5GPPPPPPPPPPPPPPPPPPGGPPGGPJB@@@@@@@@@
@@@@&#BP?:^~7!?JJJ7^^^^:!5GPY?777?JPBGY~^^^^JGGG^   !G:   YG:   YP.   55.  .5P????PGPPPPPPPPPPPPPPPPPPGGPPPGG55@@@@@@@@@
@@@&BPY7. ^!!7??J7^^^::JGY!^^^::::^^~Y#B7^^^^JGG~    .    YG.   Y5    5Y    ..   .5GPPPPPPPPPPPPPPPPPPPPPPPGPPY#@@@@@@@@
@@&#G5?^ .^~~7???~^^^^75J^^^^^!77!^^::J#G~^^^!PG5!~!!!!!!7PG7~!~PP!~~~PP?!~~^^~~!YGGGPGPPPGGGGGGGGGGPPPPPPPGGPYG@@@@@@@@
@&#BPJ!. :~~!7???^^^:JG5^^^^~YGGGG?!!!?BG~^^^7GGJ???????J5GP????????J5G5????GB5JJJ?????YPPY?777777?5PPPPPPPPGGPY&@@@@@@@
@&#G5?~  ^~~!??JJ^^^:J#5~^^^7GP555GGBB#B?^^^:?GG~:^::^:^:^5P::^::::^^^5Y:^^^P5::^::^:^:^5P::^:^^:^:~PPPPPPPPGGPJB@@@@@@@
@@&B5?^ .~~~7??JY!^^^~BG?^^^^!JJJYY555J~^^^:^5GG~^^:?P^^^:YP^^^:JY:^^^55????PY:^^^YY.^^:5G:^^^5Y.^^^5PPPPPPPPGPY5@@@@@@@
@@&#G57.:~!!?JJJYY^^^:7GGJ~:^^^^^^~~^^:^^:^75GGG~^^:7Y^^^:YP^^^:JJ:^^^5Y:^^:5Y:^^^PP~!!!GG:^^^^^:::^5GPPPPPPPGP5J@@@@@@@
@@@&&##~^777JJJJYPY~^^^~JGGY7~^::::::::^~YGBGPPG~^^^::::::5P^^^^::^^:~PY:^^:55:^^^5P7???GG:^^^?YYYYYPPPPPPPPPGPPJ&@@@@@@
@@@@@@B^^?7?YYYYYYPP7^^^:~?Y555YJJJYYYPBBBBGPPPG~^^:75YYY5GP^^^:??:^^^5Y:^^:55:^^^JJ::::PG:^^^YY!!!!PPPPPPPPPPPPJB@@@@@@
@@@@@@P^~J?JYYYYYYY5GY!^::::^^~!?Y5PGB##GGGPPPPG~^^:?BBBBGGP^^^:5P^^^^5Y^^^^55~^:::::^^!GG!^^^:::::7GPPPPPPPPPPPJB@@@@@@
@@@@@@G^~JJJYYYYYYYY5PGPJ7~^^:::::^^!?5GPGGPPPPP55555GGP5555555PG5^^^^Y5YYYYPGGPP555PPPGGBGPPPPPPPPGGPPPPPPPPPPPJB@@@@@@
@@@@@@B^~JJJYYYYYYYYYY55PGPP5J?!~^::::^~?5GPPPPP^...7P!.......:~PP~:::^^^^^~PGGGGGGGGGGGPGGGGGGGGGGGPPPPPPPPPPPPJG@@@@@@
@@@@@@B^~J?JYYYYYYYJJYYYYYJYYY5555Y?!^:::^?PPPPP?!77YP:   ~7::::YG5YJJJJJ??JPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPJG@@@@@@
@@@@@@B^~J?JYYYYYYYYJ?!~^^::::::^~?5GG?^:::!5PPP!^^~JP:   7J???YGPGGBGGGGGGGPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPJB@@@@@@
@@@@@@&!^???YYYYYYY?~:::::^^~~^^:::^?G#P~:::!YPP^   ?G!...     .5GPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPP?&@@@@@@
@@@@@@@?^7??YYYYYY!::::~?5PPPPP5J~:::!Y#P^::^?PG~   ?G5J??5P^   YGPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPPP5J@@@@@@@
@@@@@@@5^!??Y5YY57::::JBBGPP55PPGP~:::!5B!:::75B!   JB^   ::    YGPPPPPPPPPPP5YYYYPPPPPPPPPPPPPPPPPPPPPPPPPPPPPYY@@@@@@@
@@@@@@@G^~J?Y5555~:::?BPJJ??Y5YY55!:::7BB7:::?GG?~~~5BY!~~~~~~!?PPPPPPPPPPPPP5^  .5PPPPPPP5Y55PGPPPPPPPPPPPPPPPJG@@@@@@@
@@@@@@@&!^?JJ555P~:::?JP!:::~?!77~::::5&P^:::JBG?7??777??YGBJ???5P?77777777?PP:  .7777?5GP:  .7?????PPPPPPPPGPPJ&@@@@@@@
@@@@@@@@Y^!J?Y55PJ:::~5BP7^:::::::::!P#P~:::^PGG^        .5B~  .5P:        .5G:        :PB:        :PPPPPPPPPPYP@@@@@@@@
@@@@@@@@G^^J?Y55PG7:::^JGBPY?!!!!?YPBP?^:::^5BGG!   YP:  .5G?77?PG^  .YP:  .PG:  .55.  .PB:  .5PJJJJPPPPPPPPPPJB@@@@@@@@
@@@@@@@@&7^7J?555PGY~:::^!?JJYY5PPY?~::::^?GBGPB!   JP:  .5G^  .5G^  .PB:  .PG:  .GB.  .GB:  .GB.  :PPPPPPPPP5J@@@@@@@@@
@@@@@@@@@P^~JJY55PPGGJ!^:::::::::::::::~JPBGGPPB!        :5G:   YG:   5B:   5G:  .PG.  .PB:  .GG.  .PGPPPPPPP?G@@@@@@@@@
@@@@@@@@@@?^!J?Y55PPGBBGY?!~^::::^~!?YG##GPPPPPG~   !J.   YG:   YG:   ?Y.   5G:  .5G.  .PB.  .JJ   .5GPPPPPPY?@@@@@@@@@@
@@@@@@@@@@#~^???5555PPPGGBGGP^:::YBBBB##GGPPPPPG^   JB:   YG:   YG^         5P.  .5P   .PG:        ^PGPPPPPP7#@@@@@@@@@@
@@@@@@@@@@@5^~?7?55555555PPPP^:::YBPPGBBGPPPPPPG5555GG:   YGYYYYPGGYYYY?.   5B5555GG5555GBG55555555GGPPPP5P?P@@@@@@@@@@@
@@@@@@@@@@@&J7J?7?JYYYYYY5555^:::JG5PPGBPPPPPPPGGBBBBP.   .:::::YP^:::::    5BBBBBGGBBBBGGGBBBBBBBBGPPGGG5?P@@@@@@@@@@@@
@@@@@@@@@@@@@B5J?!77????JJJJY????5P55PGGP5555PPPPGGGGG5777!!!!!!5G7!!!!!777YGGGGPPPPPPP5555PPPPGGPPP5555Y5#@@@@@@@@@@@@@
@@@@@@@@@@@@@@@&BGP5Y?77!!!!7J555YJJYPGG55Y55555PPPPPGBB##BB###BGGBB###BBBBGPPPP555YYJ??77!!~!7JJY55PGB#&@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@&##GP5YYYJJJJY5PGYYYYYY555PPPPPGGGGPGGGGGGGGGGGGGGGGPPPP5YJ7~^:.....:^!JPB&@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&###BB#BYYJYYY555PPPPPPPPPPPPPPPPPPPPPPP55Y5555P5Y?!!!7??JY5PG#&&@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@5JJJYY555PPPPPPPPPPPPPP55555555PGB#&&@@@@&#BBBBBB###&&@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@5JJJJYYYY555555YYY55PGBB##&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@5J?JJJJYY55PGGB#&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@BPPGB#&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&@@&&&@&&&&&&&&&&&&&&&@@&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&@@&&@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&&&&&&&&&&&&&@@&&##BGGP55YYYYJJJYYY5PGBB#&&&&&&&&&&&&&&&&&&@@&&&&&&&@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@&&&&&&&&&&@@&&#BP5J7~^^:::::^^^^^^^^^^^:::^~!7J5GB&&&&&&&&&&&&&&&&&&&&&@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@&&&&&&&@&#G5J7^:....::::^^^~~~~~^^^^::..        .^!YG#&&&&&&&&&&&&&&&&&&@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@&&&&&&&&&@@#GY7~:........::::::^^^^^~~^^^^::.             .~JG&&&&&&&&&&&&&&&&&&&&&@@@@@@@@@@@@@@@@
&&@@&&&@@@@@&&&&&&&&&&&&&&&&&&GJ~:.....     .......::^^^^^^::.....                :!P#&&&&&&&&&&&&&&&&&&&&&&&&@@&&&&&&&&
&&@@&&@@@@@@@&&&&&&&&&&&&&@#5!:...:.....         ..::^^^^^^^:..                 ..::~JB&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&
&&&@@&@@@&&&&@@&&&&&&&&&@#5~.......    .           ..:^^^^^^::.                   .^~^7G&&&&&&&&&&&&&&&&&&&&&&&@@@@@@@@@
&&&&&&&&&&&&&&&&&&&&&&&&G!.                         ..:^^^^^:.                   ...:..Y&&&&&&&&&&&&&&&&&&&&&&&@&&&&&@@&
&&&@@@&&&&&&&&&&&&&&&@&5^                           ...::^^^:.                ....:^:::YB&&&&&&&&&&&&&&&&&&&&&&@&&&&&&@&
&&&@@&&@&&&&&&&&&&&&&&Y: ...                        ...:::....          ..:^~~~~~^^::.  ^?G&&&&&&&&&&&&&&&&&&&&@@@@@@@@&
&&&&&&&&&&&&&&&&&&&&&5^::::..                                    .:^~!7?JJJYYJ7^:.        :J#&##&&&&&&&&&&&&&&&&&@@@@@@&
&&&&&&&&&&&&&&&&&&&&P~:.                       ..........::^~~!7JY555Y5555555Y?~:.         .!G&&&&&&&&&&&&&&&&&&&&&&@@@@
&&&&&&&&&&&&&&&&&&&B!:.        ...:::::::^^^^~~!!77?????JYY555PGGPP5555555YYY5YJ!^.        ..~P&&&&&&&&&&&&&&&&&&&&&&&&&
&&&&&&&&&&&&&&&&&&&5^.       .^~!!!!!777?JJYY5555PGPPPPPGGGPPPPPPPP55P5555YYJY5Y?~^:..     :!!7G&&&&&&&&&&&&&&&&&&&&&&@&
&&&&&&&&&&&&&&&&&&B7:.      :!77?????JYYY5555PPPPGGGPPPPPGPPPPPPPPPPPP55555YYY55J!^:^^:   .~?J?JB&&&&&&&&&&&&&&&&&&&&&&&
&&&&&&&&&&&&&&&&&&P^.      .7JJ?JJJJJJYY55555PPPPPPPGGGGGGGPPPPGGGP5PPYYYY55YYYYY7^:.:~~::^!??JJP#&&&&&&&&&&&&&&&&&&&&&&
&&&&&&&&&&&&&&&&&&J:       ^Y5YJJJYJYJYYYY5555PPPPPGGGBBBBGGGGGGGGPPPYY555Y5YJJJJ?~:. :~!!!77?J7JB&&&&&&&&&&&&@&&&@@@@&&
&&&&&&&&&&&&&&&&&&J:      .!PGYJJYJJYJJYYY55PPPPPPGGGGGBBBGGBBBBBBGG55555YYYJJJJJJ7^.   ~7777?J!7B&&&&&&&&&&&&@&&&&@@@&&
&&&&&&&&&&&&&&&&&&?.     .^?PBG5YYJJYYYYY555PPPPPGGGGGGBBBBBBBBBBBGGPP5555Y5YJJJJJ7~:   :!!7!!J!!G&&&&&&&&&&&&&&&&&@@@&&
&&&&&&&&&&&&&&&&&&! ..    ~JPGGPYYYJJJYYY55PPPPPPPPPGGGGGGGGGGGGPPPPP55Y55YYYJJJYJ?!^.  .~~7!!J!~P&&&&&&&&&&&&@@&&&@@@&&
&&&&&&&&&&&&&&&&&&7 ...   ~YPGGPYYJJJJYYYY5PPPPPPGPPPGGGGGGGGGPPPPPP55P5555YYYYJYJJ?7:  .^~!!!!~!G@&&&&&&&&&&&@@&&&&@@&&
&&&&&&&&&&&&&&&&&&5: ..   ^JPPP5JYYJJJYYYY5555PPPGGPGGGGGGPPPPPPP55P55555555555JJJJJ?^   .:^~~~^75B&&&&&&&&&&&&&&&&&@@&&
&&&&&&&&&&&&&&&&&&#J.     !J5PPYJJJJYJJJYYYY5PPPPGGPPGGPPPPPGGGGPPPP555YYYY55PPP5YJ??!.   .::^^!J7JB&&&&&&&&&&&&&&&&&&&&
&&&&&&&&&&&&&&&&&&&&5^    !Y5PP5YJJJJJYYYYY55PPPPGGPPGGGGGGGGGGGGGP5J????JJJY55PP5YJ?!.   .. :!JYJJB&&&&&&&&&&&&&&&&&&&&
&&&&&&&&&&&&&&&&&&&&@P^.  ~555PYJ??JJJJYYY55PPP5PPP5GGPPGGPPP5YYYJ?77!!777???JY5PP55Y!.   ...7Y5555#&&&&&&&&&&&&&&&&&&&&
@@@@@@@&&@@&&@&&&&&&@#?.. :YP5Y?7!~~!!!!!77?JYYYY5Y5PP55P5YYJ7~^::^~!7?J???777?J55PGGJ:  .::^?Y5P5G&&&&&&&&&&&&&&&&&&&&&
@@@@@@@@@@@@@&&&&&&&&@B7...JPY?!~^^^^:::::^^~!??JJJY555YYYJ?77!!~~!7!77??JJJ?7?YY555GP!. .^7?7?Y55B&&&&&&&&&&&&&&&&&@&&&
@@@@@@@@@@@@@@@@@@&&&@@G!:.75Y?77!!!~^:::....:^~~7?JY555YJ777?7!~!^... ^~~!?YJY5Y555PPJ^ .!GP?7Y55#&&&&&&&&&&&&&&&&&@&&&
@@@@@@@@@@@@@@@@@@@@&&@&5^.75YJ7?7!::^!:..::!7~~~~!?Y5PP5JJJJJJJ5GY!!7?55J7!7??JJY5PPGG!.:JB5?JYYP#&&&&&&&&&&&&&&&&&@&&&
@@@@@@@@@@@@@@@@@@@&@&@@G!.!55J7?!^!?Y5J77?YPP5YJ7!JPGGPYJ555YJJYY5PPPGP5YJYYYYJJJY5PGP?~?5YY5YYYP#&&&&&&&&&&&&&&&&@@&&&
@@@@@@@@@@@@@@@@@@@@@&@@BJ^~Y5Y????JYYYY5555YYYJ7!!J5PP5JJY5YYYYYYYYJJYYY555YJJJJY5PPP5YPP555Y555P&&&&&&&&&&&&&&&&&@@&&&
@@@@@@@@@@@@@@@@@@@@@@@@BYJ!?JYJJ???????J?JJ55YJ77JY55P5YJY555PPPPPPPPPPPPPP5YYJJYYPG5YYPPYP555PPP&&&&&&&&&&&&&&&&&@@&&&
@@@@@@@@@@@@@@@@@@@@@&@@PJY??JJYJJJYYY55555YYYYJ?JYY55P5Y5555PPPGGGGGGGGGPP55YYYY5PPPYJYPPYY5GP55B&&&&&&&&&&&&&&&&&@@&&&
@@@@@@@@@@@@@@@@@@@@@&@@PJY?7JJYYYJJYYYYYYYYYYY55YYY5PPP5PPPP555PGBBBB##BBGP5YYY5PPP5YJYPPYY5P5YG&&&&&&&&&&&&&&&@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@&@@#5J??JJ5PYJYYY5PPPP5YJ7J55Y55GGPP555P55J??Y5PGGBGGP55YJY55YYYYYY55JYY5PB&&&&&&&&&&&&&&&&@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@&@@#Y77?JPG5YYY55555YJ?JJJ5GP5PBBG555PP55Y?!~~7JYYYYYYYJJJYYJJJYJY55G##&&&&&&&&&&&&&&&&&&&@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@&GYJJY5YJJYYYYJ?!~!Y555GG5PB#G555P5YJYYY?!~^~7?JJJJJ??????JJYYYYJ5#&&&&&&&&&&&&&&&&&&@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@&&&@@#YJJJJ??JJJJ7~~~~!7?Y55Y5GGPYJJ?77JYYYYYJ7~^~!7???777????JJYYY~^JG&&&&&&&&&&&&&&&&&&@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@&&&&@#Y????777?7!~!!!!~~~~!7????7777?JYY555YYYYY?!^^!??77??J?JJJJJYJ!75B#&&&&&&&&&&&&&&&&&@@@@@@@
@@@@@@@@@@@@@@@@@@@@@&&&&&&@&P?777??77!!!!!!!!!!~~~~!~!77??JYY5YYYJJ?7!!!77!?J??JYJJJJJJJJ7^7YPBB#&&&&&&&&&&&&&&@@@@@@@@
@@@@@@@@@@@@@@@@@@&&@&&&&&&&@#J77!7?JJJJ?!~::~!7!!77?JY5YYYJJJ?JYJJ?:^7JYYJYYJ?JYYJ?JJ??J?:.75PBBB#&&&&&&&&&&&&&@@@@@@@@
@@@@@@@@@@@@@@@@@@&&@@&&&&&&&&BJ7!!7JY5YYJ?!^:^?Y??J?JJJJJYJ5P5B#PYJYPG5YYYYYJJJYYJ?????PB~^J5GBBB####&&&&&&&@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@&&&&&&&&&&@@GJ7!77?Y55JJYJ?!~J5YG55G5PB#GBGJY5?JPGP5YYYYYYJ?JYJJ?????PBJ75PGGBBBBB##&&&&@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@&&&&&&&&&&&&&&BY7777?JYYYYY?7?JJJ5YYY555YJYY5P5555P55YYYYJJ??JJ?????!7~~7Y5PPGGGGGBB####BB##&&&@@@@&@@
@@@@@@@@@@@@@@@@@&&&&&&&&&&&&&&&@#5?7777?JJYYYJJJJJY5PPPGGPGGGP5YYY5555P5YYJ??JJ??????YP~:7Y55PGGGGGBBBB###BGPPPGB#&&&&@
@@@@@@@@@@@@&&&&&&&&&&&&&&&&&&##BBG5?7777?JYYYYJ??777?JJYYYYJJJJJY5555P55YJ?7??777?7?B@5^!Y55PPPGGGGGBBBB###BGPPGGB#&&&&
@@@@@@@@@@@@@@@&&&&&&&&&&&&#BPP55YYY?7777??JYYYYJJ???????JJJY555PP55PP5YJJ?77?77777?GB&BYJY55PPPPGGGGBBBBBB#BGB#GPG#&&&&
@@@@@@@@@@@@@@@@@&@&&@@&&BG55YYYJJJJ?7?J?7?JJYY5YYYYYYY5555PGGGGPPPPP5YYJ?77?777!~^!!^!!?JY5PPPPGGGGGBBBBGBBBGGGGB#&&&&&
@@@@@@@@@@@@@@@@@&@@@&#GP5YYYJJJJYY??JYYYJ7??JY5YYYYYY5PPGPGGGGPPP55YYJ??77!!~~~77??!:.~?YY5PPPGGGGGGBBBBGB#B5PB##&&&&&&
@@@@@@@@@@@@@@@@@@@&#G5YYJJJ???JYPYYYYYP5!!5??JYYYYJJJJY5555555555YJJ?77!~~^^^!5&@&J::~?Y5PPGGGGGGGGGGBBBGGGGG##&&&&&&&&
@@@@@@@@@@@@@@@@@@#G555YJJJ?777JPP5555P5?~!!~^^!??JJJ???77??JJJJ??7!!!~~^::^~?GBB#5JY7?Y5PPGGPGGGGGGGGBBGGPGBB###&&&&&&&
@@@@@@@@@@@@@@@@&GYY55YJJ??777J5PPPP5P5?~!~.    :^!!~~~~~^^~~~~^^^^:^^^:::::^~?!^~^!7?Y5PGGGGGGGGGGGGBBP5Y5PPGBB#######B
@@@@@@@@@@@@@@@#PJJ5PPYJ?77!!7YPPGPP55J~~7!..:^:. ~7!^:..     ....:^^^:^7PBPYY!:. ~?JY5PGGGGGGGGGGGGGGPYJYYYY55PGBB#####
@@@@@@@@@@@@@@#5J?JPGPYJ?7!!7YPGGGPP5Y7~~Y?::::^~: ^!!~::::::::::.:::^?#@@@@B~ ..~7JYPGGGGGGBBGGGBBGG5JJYY5YYYYYY55PPGBB
@@@@@@@@@@@@@BY?7?YPGPY?7!!!YPGGPPPP57!~~?!~   .^.  .^~^. ...... .^^^7PPY5&BJJ5?7JY5PGGGBBBGGBBGGGGG5JJYY5YYYYYY555555PP

Bob Barker is a hundred fuckin years today so we're day-drinking and launching this shindig, 

get ready, THE PRICE IS RIGHT AF!  Less fuckin send this filthy bitch you degenerate legends.

Stay tuned on our Discord

RIP Bob Barker 12/12/1923-12/12/2023

https://en.wikipedia.org/wiki/Bob_Barker 

*/

// SPDX-License-Identifier: Unlicensed

pragma solidity 0.8.11;
 
abstract contract Context {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }
 
    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}
 
interface IUniswapV2Pair {
    event Approval(address indexed owner, address indexed spender, uint value);
    event Transfer(address indexed from, address indexed to, uint value);
 
    function name() external pure returns (string memory);
    function symbol() external pure returns (string memory);
    function decimals() external pure returns (uint8);
    function totalSupply() external view returns (uint);
    function balanceOf(address owner) external view returns (uint);
    function allowance(address owner, address spender) external view returns (uint);
 
    function approve(address spender, uint value) external returns (bool);
    function transfer(address to, uint value) external returns (bool);
    function transferFrom(address from, address to, uint value) external returns (bool);
 
    function DOMAIN_SEPARATOR() external view returns (bytes32);
    function PERMIT_TYPEHASH() external pure returns (bytes32);
    function nonces(address owner) external view returns (uint);
 
    function permit(address owner, address spender, uint value, uint deadline, uint8 v, bytes32 r, bytes32 s) external;
 
    event Mint(address indexed sender, uint amount0, uint amount1);
    event Swap(
        address indexed sender,
        uint amount0In,
        uint amount1In,
        uint amount0Out,
        uint amount1Out,
        address indexed to
    );
    event Sync(uint112 reserve0, uint112 reserve1);
 
    function MINIMUM_LIQUIDITY() external pure returns (uint);
    function factory() external view returns (address);
    function token0() external view returns (address);
    function token1() external view returns (address);
    function getReserves() external view returns (uint112 reserve0, uint112 reserve1, uint32 blockTimestampLast);
    function price0CumulativeLast() external view returns (uint);
    function price1CumulativeLast() external view returns (uint);
    function kLast() external view returns (uint);
 
    function mint(address to) external returns (uint liquidity);
    function burn(address to) external returns (uint amount0, uint amount1);
    function swap(uint amount0Out, uint amount1Out, address to, bytes calldata data) external;
    function skim(address to) external;
    function sync() external;
 
    function initialize(address, address) external;
}
 
interface IUniswapV2Factory {
    event PairCreated(address indexed token0, address indexed token1, address pair, uint);
 
    function feeTo() external view returns (address);
    function feeToSetter() external view returns (address);
 
    function getPair(address tokenA, address tokenB) external view returns (address pair);
    function allPairs(uint) external view returns (address pair);
    function allPairsLength() external view returns (uint);
 
    function createPair(address tokenA, address tokenB) external returns (address pair);
 
    function setFeeTo(address) external;
    function setFeeToSetter(address) external;
}
 
interface IERC20 {

    function totalSupply() external view returns (uint256);

    function balanceOf(address account) external view returns (uint256);

    function transfer(address recipient, uint256 amount) external returns (bool);

    function allowance(address owner, address spender) external view returns (uint256);

    function approve(address spender, uint256 amount) external returns (bool);

    function transferFrom(
        address sender,
        address recipient,
        uint256 amount
    ) external returns (bool);

    event Transfer(address indexed from, address indexed to, uint256 value);

    event Approval(address indexed owner, address indexed spender, uint256 value);
}
 
interface IERC20Metadata is IERC20 {

    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}
 
 
contract ERC20 is Context, IERC20, IERC20Metadata {
    using SafeMath for uint256;
 
    mapping(address => uint256) private _balances;
 
    mapping(address => mapping(address => uint256)) private _allowances;
 
    uint256 private _totalSupply;
 
    string private _name;
    string private _symbol;

    constructor(string memory name_, string memory symbol_) {
        _name = name_;
        _symbol = symbol_;
    }

    function name() public view virtual override returns (string memory) {
        return _name;
    }

    function symbol() public view virtual override returns (string memory) {
        return _symbol;
    }

    function decimals() public view virtual override returns (uint8) {
        return 18;
    }

    function totalSupply() public view virtual override returns (uint256) {
        return _totalSupply;
    }

    function balanceOf(address account) public view virtual override returns (uint256) {
        return _balances[account];
    }

    function transfer(address recipient, uint256 amount) public virtual override returns (bool) {
        _transfer(_msgSender(), recipient, amount);
        return true;
    }

    function allowance(address owner, address spender) public view virtual override returns (uint256) {
        return _allowances[owner][spender];
    }

    function approve(address spender, uint256 amount) public virtual override returns (bool) {
        _approve(_msgSender(), spender, amount);
        return true;
    }

    function transferFrom(
        address sender,
        address recipient,
        uint256 amount
    ) public virtual override returns (bool) {
        _transfer(sender, recipient, amount);
        _approve(sender, _msgSender(), _allowances[sender][_msgSender()].sub(amount, "ERC20: transfer amount exceeds allowance"));
        return true;
    }

    function increaseAllowance(address spender, uint256 addedValue) public virtual returns (bool) {
        _approve(_msgSender(), spender, _allowances[_msgSender()][spender].add(addedValue));
        return true;
    }

    function decreaseAllowance(address spender, uint256 subtractedValue) public virtual returns (bool) {
        _approve(_msgSender(), spender, _allowances[_msgSender()][spender].sub(subtractedValue, "ERC20: decreased allowance below zero"));
        return true;
    }

    function _transfer(
        address sender,
        address recipient,
        uint256 amount
    ) internal virtual {
        require(sender != address(0), "ERC20: transfer from the zero address");
        require(recipient != address(0), "ERC20: transfer to the zero address");
 
        _beforeTokenTransfer(sender, recipient, amount);
 
        _balances[sender] = _balances[sender].sub(amount, "ERC20: transfer amount exceeds balance");
        _balances[recipient] = _balances[recipient].add(amount);
        emit Transfer(sender, recipient, amount);
    }

    function _mint(address account, uint256 amount) internal virtual {
        require(account != address(0), "ERC20: mint to the zero address");
 
        _beforeTokenTransfer(address(0), account, amount);
 
        _totalSupply = _totalSupply.add(amount);
        _balances[account] = _balances[account].add(amount);
        emit Transfer(address(0), account, amount);
    }

    function _burn(address account, uint256 amount) internal virtual {
        require(account != address(0), "ERC20: burn from the zero address");
 
        _beforeTokenTransfer(account, address(0), amount);
 
        _balances[account] = _balances[account].sub(amount, "ERC20: burn amount exceeds balance");
        _totalSupply = _totalSupply.sub(amount);
        emit Transfer(account, address(0), amount);
    }

    function _approve(
        address owner,
        address spender,
        uint256 amount
    ) internal virtual {
        require(owner != address(0), "ERC20: approve from the zero address");
        require(spender != address(0), "ERC20: approve to the zero address");
 
        _allowances[owner][spender] = amount;
        emit Approval(owner, spender, amount);
    }

    function _beforeTokenTransfer(
        address from,
        address to,
        uint256 amount
    ) internal virtual {}
}
 
library SafeMath {

    function add(uint256 a, uint256 b) internal pure returns (uint256) {
        uint256 c = a + b;
        require(c >= a, "SafeMath: addition overflow");
 
        return c;
    }

    function sub(uint256 a, uint256 b) internal pure returns (uint256) {
        return sub(a, b, "SafeMath: subtraction overflow");
    }

    function sub(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b <= a, errorMessage);
        uint256 c = a - b;
 
        return c;
    }

    function mul(uint256 a, uint256 b) internal pure returns (uint256) {

        if (a == 0) {
            return 0;
        }
 
        uint256 c = a * b;
        require(c / a == b, "SafeMath: multiplication overflow");
 
        return c;
    }

    function div(uint256 a, uint256 b) internal pure returns (uint256) {
        return div(a, b, "SafeMath: division by zero");
    }

    function div(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b > 0, errorMessage);
        uint256 c = a / b;
        // assert(a == b * c + a % b); // There is no case in which this doesn't hold
 
        return c;
    }

    function mod(uint256 a, uint256 b) internal pure returns (uint256) {
        return mod(a, b, "SafeMath: modulo by zero");
    }

    function mod(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b != 0, errorMessage);
        return a % b;
    }
}
 
contract Ownable is Context {
    address private _owner;
 
    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

    constructor () {
        address msgSender = _msgSender();
        _owner = msgSender;
        emit OwnershipTransferred(address(0), msgSender);
    }

    function owner() public view returns (address) {
        return _owner;
    }

    modifier onlyOwner() {
        require(_owner == _msgSender(), "Ownable: caller is not the owner");
        _;
    }

    function renounceOwnership() public virtual onlyOwner {
        emit OwnershipTransferred(_owner, address(0));
        _owner = address(0);
    }

    function transferOwnership(address newOwner) public virtual onlyOwner {
        require(newOwner != address(0), "Ownable: new owner is the zero address");
        emit OwnershipTransferred(_owner, newOwner);
        _owner = newOwner;
    }
}
 
 
 
library SafeMathInt {
    int256 private constant MIN_INT256 = int256(1) << 255;
    int256 private constant MAX_INT256 = ~(int256(1) << 255);

    function mul(int256 a, int256 b) internal pure returns (int256) {
        int256 c = a * b;
 
        // Detect overflow when multiplying MIN_INT256 with -1
        require(c != MIN_INT256 || (a & MIN_INT256) != (b & MIN_INT256));
        require((b == 0) || (c / b == a));
        return c;
    }

    function div(int256 a, int256 b) internal pure returns (int256) {
        // Prevent overflow when dividing MIN_INT256 by -1
        require(b != -1 || a != MIN_INT256);
 
        // Solidity already throws when dividing by 0.
        return a / b;
    }

    function sub(int256 a, int256 b) internal pure returns (int256) {
        int256 c = a - b;
        require((b >= 0 && c <= a) || (b < 0 && c > a));
        return c;
    }

    function add(int256 a, int256 b) internal pure returns (int256) {
        int256 c = a + b;
        require((b >= 0 && c >= a) || (b < 0 && c < a));
        return c;
    }

    function abs(int256 a) internal pure returns (int256) {
        require(a != MIN_INT256);
        return a < 0 ? -a : a;
    }
 
 
    function toUint256Safe(int256 a) internal pure returns (uint256) {
        require(a >= 0);
        return uint256(a);
    }
}
 
library SafeMathUint {
  function toInt256Safe(uint256 a) internal pure returns (int256) {
    int256 b = int256(a);
    require(b >= 0);
    return b;
  }
}
 
 
interface IUniswapV2Router01 {
    function factory() external pure returns (address);
    function WETH() external pure returns (address);
 
    function addLiquidity(
        address tokenA,
        address tokenB,
        uint amountADesired,
        uint amountBDesired,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB, uint liquidity);
    function addLiquidityETH(
        address token,
        uint amountTokenDesired,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external payable returns (uint amountToken, uint amountETH, uint liquidity);
    function removeLiquidity(
        address tokenA,
        address tokenB,
        uint liquidity,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB);
    function removeLiquidityETH(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external returns (uint amountToken, uint amountETH);
    function removeLiquidityWithPermit(
        address tokenA,
        address tokenB,
        uint liquidity,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountA, uint amountB);
    function removeLiquidityETHWithPermit(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountToken, uint amountETH);
    function swapExactTokensForTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external returns (uint[] memory amounts);
    function swapTokensForExactTokens(
        uint amountOut,
        uint amountInMax,
        address[] calldata path,
        address to,
        uint deadline
    ) external returns (uint[] memory amounts);
    function swapExactETHForTokens(uint amountOutMin, address[] calldata path, address to, uint deadline)
        external
        payable
        returns (uint[] memory amounts);
    function swapTokensForExactETH(uint amountOut, uint amountInMax, address[] calldata path, address to, uint deadline)
        external
        returns (uint[] memory amounts);
    function swapExactTokensForETH(uint amountIn, uint amountOutMin, address[] calldata path, address to, uint deadline)
        external
        returns (uint[] memory amounts);
    function swapETHForExactTokens(uint amountOut, address[] calldata path, address to, uint deadline)
        external
        payable
        returns (uint[] memory amounts);
 
    function quote(uint amountA, uint reserveA, uint reserveB) external pure returns (uint amountB);
    function getAmountOut(uint amountIn, uint reserveIn, uint reserveOut) external pure returns (uint amountOut);
    function getAmountIn(uint amountOut, uint reserveIn, uint reserveOut) external pure returns (uint amountIn);
    function getAmountsOut(uint amountIn, address[] calldata path) external view returns (uint[] memory amounts);
    function getAmountsIn(uint amountOut, address[] calldata path) external view returns (uint[] memory amounts);
}
 
interface IUniswapV2Router02 is IUniswapV2Router01 {
    function removeLiquidityETHSupportingFeeOnTransferTokens(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external returns (uint amountETH);
    function removeLiquidityETHWithPermitSupportingFeeOnTransferTokens(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountETH);
 
    function swapExactTokensForTokensSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
    function swapExactETHForTokensSupportingFeeOnTransferTokens(
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external payable;
    function swapExactTokensForETHSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
}
 
contract ThePriceIsRightBBC is ERC20, Ownable {
    using SafeMath for uint256;
 
    IUniswapV2Router02 public immutable uniswapV2Router;
    address public immutable uniswapV2Pair;
 
    bool private swapping;
 
    address private marketingWallet;
    address private devWallet;
 
    uint256 public maxTransactionAmount;
    uint256 public swapTokensAtAmount;
    uint256 public maxWallet;
 
    bool public limitsInEffect = true;
    bool public tradingActive = false;
    bool public swapEnabled = false;
 
     // Anti-bot and anti-whale mappings and variables
    mapping(address => uint256) private _holderLastTransferTimestamp; // to hold last Transfers temporarily during launch
 
    // Seller Map
    mapping (address => uint256) private _holderFirstBuyTimestamp;
 
    // Blacklist Map
    mapping (address => bool) private _blacklist;
    bool public transferDelayEnabled = true;
 
    uint256 public buyTotalFees;
    uint256 public buyMarketingFee;
    uint256 public buyLiquidityFee;
    uint256 public buyDevFee;
 
    uint256 public sellTotalFees;
    uint256 public sellMarketingFee;
    uint256 public sellLiquidityFee;
    uint256 public sellDevFee;
 
    uint256 public tokensForMarketing;
    uint256 public tokensForLiquidity;
    uint256 public tokensForDev;
 
    // block number of opened trading
    uint256 launchedAt;
 
    /******************/
 
    // exclude from fees and max transaction amount
    mapping (address => bool) private _isExcludedFromFees;
    mapping (address => bool) public _isExcludedMaxTransactionAmount;
 
    // store addresses that a automatic market maker pairs. Any transfer *to* these addresses
    // could be subject to a maximum transfer amount
    mapping (address => bool) public automatedMarketMakerPairs;
 
    event UpdateUniswapV2Router(address indexed newAddress, address indexed oldAddress);
 
    event ExcludeFromFees(address indexed account, bool isExcluded);
 
    event SetAutomatedMarketMakerPair(address indexed pair, bool indexed value);
 
    event marketingWalletUpdated(address indexed newWallet, address indexed oldWallet);
 
    event devWalletUpdated(address indexed newWallet, address indexed oldWallet);
 
    event SwapAndLiquify(
        uint256 tokensSwapped,
        uint256 ethReceived,
        uint256 tokensIntoLiquidity
    );
 
    event AutoNukeLP();
 
    event ManualNukeLP();
 
    constructor() ERC20("Bob Barker Coin", unicode"🅱️") {
 
        IUniswapV2Router02 _uniswapV2Router = IUniswapV2Router02(0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D);
 
        excludeFromMaxTransaction(address(_uniswapV2Router), true);
        uniswapV2Router = _uniswapV2Router;
 
        uniswapV2Pair = IUniswapV2Factory(_uniswapV2Router.factory()).createPair(address(this), _uniswapV2Router.WETH());
        excludeFromMaxTransaction(address(uniswapV2Pair), true);
        _setAutomatedMarketMakerPair(address(uniswapV2Pair), true);
 
        uint256 _buyMarketingFee = 15;
        uint256 _buyLiquidityFee = 0;
        uint256 _buyDevFee = 10;
 
        uint256 _sellMarketingFee = 25;
        uint256 _sellLiquidityFee = 0;
        uint256 _sellDevFee = 25;
 
        uint256 totalSupply = 223223223 * 1e18;
 
        maxTransactionAmount = totalSupply * 10 / 1000; // 2%
        maxWallet = totalSupply * 20 / 1000; // 2% 
        swapTokensAtAmount = totalSupply * 10 / 1000; // 1%
 
        buyMarketingFee = _buyMarketingFee;
        buyLiquidityFee = _buyLiquidityFee;
        buyDevFee = _buyDevFee;
        buyTotalFees = buyMarketingFee + buyLiquidityFee + buyDevFee;
 
        sellMarketingFee = _sellMarketingFee;
        sellLiquidityFee = _sellLiquidityFee;
        sellDevFee = _sellDevFee;
        sellTotalFees = sellMarketingFee + sellLiquidityFee + sellDevFee;
 
        marketingWallet = address(0x6641CcAFd12b3a7Fe426311987C67Db33c64026a);
        devWallet = address(0x1e32cE52389367390c74941A6792215B8D4760aC);
 
        // exclude from paying fees or having max transaction amount
        excludeFromFees(owner(), true);
        excludeFromFees(address(this), true);
        excludeFromFees(address(0xdead), true);
        excludeFromFees(address(marketingWallet), true);
 
        excludeFromMaxTransaction(owner(), true);
        excludeFromMaxTransaction(address(this), true);
        excludeFromMaxTransaction(address(0xdead), true);
        excludeFromMaxTransaction(address(devWallet), true);
        excludeFromMaxTransaction(address(marketingWallet), true);
 
        /*
            _mint is an internal function in ERC20.sol that is only called here,
            and CANNOT be called ever again
        */
        _mint(msg.sender, totalSupply);
    }
 
    receive() external payable {
 
    }
 
    // once enabled, can never be turned off
    function enableTrading() external onlyOwner {
        tradingActive = true;
        swapEnabled = true;
        launchedAt = block.number;
    }
 
    // remove limits after token is stable
    function removeLimits() external onlyOwner returns (bool){
        limitsInEffect = false;
        return true;
    }
 
    // disable Transfer delay - cannot be reenabled
    function disableTransferDelay() external onlyOwner returns (bool){
        transferDelayEnabled = false;
        return true;
    }
 
     // change the minimum amount of tokens to sell from fees
    function updateSwapTokensAtAmount(uint256 newAmount) external onlyOwner returns (bool){
        require(newAmount >= totalSupply() * 1 / 100000, "Swap amount cannot be lower than 0.001% total supply.");
        require(newAmount <= totalSupply() * 20 / 1000, "Swap amount cannot be higher than 2% total supply.");
        swapTokensAtAmount = newAmount;
        return true;
    }
 
    function updateMaxTxnAmount(uint256 newNum) external onlyOwner {
        require(newNum >= (totalSupply() * 1 / 1000)/1e18, "Cannot set maxTransactionAmount lower than 0.1%");
        maxTransactionAmount = newNum * (10**18);
    }
 
    function updateMaxWalletAmount(uint256 newNum) external onlyOwner {
        require(newNum >= (totalSupply() * 5 / 1000)/1e18, "Cannot set maxWallet lower than 0.5%");
        maxWallet = newNum * (10**18);
    }
 
    function excludeFromMaxTransaction(address updAds, bool isEx) public onlyOwner {
        _isExcludedMaxTransactionAmount[updAds] = isEx;
    }

    function updateBuyFees(
        uint256 _devFee,
        uint256 _liquidityFee,
        uint256 _marketingFee
    ) external onlyOwner {
        buyDevFee = _devFee;
        buyLiquidityFee = _liquidityFee;
        buyMarketingFee = _marketingFee;
        buyTotalFees = buyDevFee + buyLiquidityFee + buyMarketingFee;
    }

    function updateSellFees(
        uint256 _devFee,
        uint256 _liquidityFee,
        uint256 _marketingFee
    ) external onlyOwner {
        sellDevFee = _devFee;
        sellLiquidityFee = _liquidityFee;
        sellMarketingFee = _marketingFee;
        sellTotalFees = sellDevFee + sellLiquidityFee + sellMarketingFee;
    }
 
    // only use to disable contract sales if absolutely necessary (emergency use only)
    function updateSwapEnabled(bool enabled) external onlyOwner(){
        swapEnabled = enabled;
    }
 
    function excludeFromFees(address account, bool excluded) public onlyOwner {
        _isExcludedFromFees[account] = excluded;
        emit ExcludeFromFees(account, excluded);
    }
 
    function blacklistAccount (address account, bool isBlacklisted) public onlyOwner {
        _blacklist[account] = isBlacklisted;
    }
 
    function setAutomatedMarketMakerPair(address pair, bool value) public onlyOwner {
        require(pair != uniswapV2Pair, "The pair cannot be removed from automatedMarketMakerPairs");
 
        _setAutomatedMarketMakerPair(pair, value);
    }
 
    function _setAutomatedMarketMakerPair(address pair, bool value) private {
        automatedMarketMakerPairs[pair] = value;
 
        emit SetAutomatedMarketMakerPair(pair, value);
    }
 
    function updateMarketingWallet(address newMarketingWallet) external onlyOwner {
        emit marketingWalletUpdated(newMarketingWallet, marketingWallet);
        marketingWallet = newMarketingWallet;
    }
 
    function updateDevWallet(address newWallet) external onlyOwner {
        emit devWalletUpdated(newWallet, devWallet);
        devWallet = newWallet;
    }
 
 
    function isExcludedFromFees(address account) public view returns(bool) {
        return _isExcludedFromFees[account];
    }
 
    function _transfer(
        address from,
        address to,
        uint256 amount
    ) internal override {
        require(from != address(0), "ERC20: transfer from the zero address");
        require(to != address(0), "ERC20: transfer to the zero address");
        require(!_blacklist[to] && !_blacklist[from], "You have been blacklisted from transfering tokens");
         if(amount == 0) {
            super._transfer(from, to, 0);
            return;
        }
 
        if(limitsInEffect){
            if (
                from != owner() &&
                to != owner() &&
                to != address(0) &&
                to != address(0xdead) &&
                !swapping
            ){
                if(!tradingActive){
                    require(_isExcludedFromFees[from] || _isExcludedFromFees[to], "Trading is not active.");
                }
 
                // at launch if the transfer delay is enabled, ensure the block timestamps for purchasers is set -- during launch.  
                if (transferDelayEnabled){
                    if (to != owner() && to != address(uniswapV2Router) && to != address(uniswapV2Pair)){
                        require(_holderLastTransferTimestamp[tx.origin] < block.number, "_transfer:: Transfer Delay enabled.  Only one purchase per block allowed.");
                        _holderLastTransferTimestamp[tx.origin] = block.number;
                    }
                }
 
                //when buy
                if (automatedMarketMakerPairs[from] && !_isExcludedMaxTransactionAmount[to]) {
                        require(amount <= maxTransactionAmount, "Buy transfer amount exceeds the maxTransactionAmount.");
                        require(amount + balanceOf(to) <= maxWallet, "Max wallet exceeded");
                }
 
                //when sell
                else if (automatedMarketMakerPairs[to] && !_isExcludedMaxTransactionAmount[from]) {
                        require(amount <= maxTransactionAmount, "Sell transfer amount exceeds the maxTransactionAmount.");
                }
                else if(!_isExcludedMaxTransactionAmount[to]){
                    require(amount + balanceOf(to) <= maxWallet, "Max wallet exceeded");
                }
            }
        }
 
        uint256 contractTokenBalance = balanceOf(address(this));
 
        bool canSwap = contractTokenBalance >= swapTokensAtAmount;
 
        if( 
            canSwap &&
            swapEnabled &&
            !swapping &&
            !automatedMarketMakerPairs[from] &&
            !_isExcludedFromFees[from] &&
            !_isExcludedFromFees[to]
        ) {
            swapping = true;
 
            swapBack();
 
            swapping = false;
        }
 
        bool takeFee = !swapping;
 
        // if any account belongs to _isExcludedFromFee account then remove the fee
        if(_isExcludedFromFees[from] || _isExcludedFromFees[to]) {
            takeFee = false;
        }
 
        uint256 fees = 0;
        // only take fees on buys/sells, do not take on wallet transfers
        if(takeFee){
            // on sell
            if (automatedMarketMakerPairs[to] && sellTotalFees > 0){
                fees = amount.mul(sellTotalFees).div(100);
                tokensForLiquidity += fees * sellLiquidityFee / sellTotalFees;
                tokensForDev += fees * sellDevFee / sellTotalFees;
                tokensForMarketing += fees * sellMarketingFee / sellTotalFees;
            }
            // on buy
            else if(automatedMarketMakerPairs[from] && buyTotalFees > 0) {
                fees = amount.mul(buyTotalFees).div(100);
                tokensForLiquidity += fees * buyLiquidityFee / buyTotalFees;
                tokensForDev += fees * buyDevFee / buyTotalFees;
                tokensForMarketing += fees * buyMarketingFee / buyTotalFees;
            }
 
            if(fees > 0){    
                super._transfer(from, address(this), fees);
            }
 
            amount -= fees;
        }
 
        super._transfer(from, to, amount);
    }
 
    function swapTokensForEth(uint256 tokenAmount) private {
 
        // generate the uniswap pair path of token -> weth
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = uniswapV2Router.WETH();
 
        _approve(address(this), address(uniswapV2Router), tokenAmount);
 
        // make the swap
        uniswapV2Router.swapExactTokensForETHSupportingFeeOnTransferTokens(
            tokenAmount,
            0, // accept any amount of ETH
            path,
            address(this),
            block.timestamp
        );
 
    }
 
    function addLiquidity(uint256 tokenAmount, uint256 ethAmount) private {
        // approve token transfer to cover all possible scenarios
        _approve(address(this), address(uniswapV2Router), tokenAmount);
 
        // add the liquidity
        uniswapV2Router.addLiquidityETH{value: ethAmount}(
            address(this),
            tokenAmount,
            0, // slippage is unavoidable
            0, // slippage is unavoidable
            address(this),
            block.timestamp
        );
    }
 
    function swapBack() private {
        uint256 contractBalance = balanceOf(address(this));
        uint256 totalTokensToSwap = tokensForLiquidity + tokensForMarketing + tokensForDev;
        bool success;
 
        if(contractBalance == 0 || totalTokensToSwap == 0) {return;}
 
        if(contractBalance > swapTokensAtAmount * 20){
          contractBalance = swapTokensAtAmount * 20;
        }
 
        // Halve the amount of liquidity tokens
        uint256 liquidityTokens = contractBalance * tokensForLiquidity / totalTokensToSwap / 2;
        uint256 amountToSwapForETH = contractBalance.sub(liquidityTokens);
 
        uint256 initialETHBalance = address(this).balance;
 
        swapTokensForEth(amountToSwapForETH); 
 
        uint256 ethBalance = address(this).balance.sub(initialETHBalance);
 
        uint256 ethForMarketing = ethBalance.mul(tokensForMarketing).div(totalTokensToSwap);
        uint256 ethForDev = ethBalance.mul(tokensForDev).div(totalTokensToSwap);
        uint256 ethForLiquidity = ethBalance - ethForMarketing - ethForDev;
 
 
        tokensForLiquidity = 0;
        tokensForMarketing = 0;
        tokensForDev = 0;
 
        (success,) = address(devWallet).call{value: ethForDev}("");
 
        if(liquidityTokens > 0 && ethForLiquidity > 0){
            addLiquidity(liquidityTokens, ethForLiquidity);
            emit SwapAndLiquify(amountToSwapForETH, ethForLiquidity, tokensForLiquidity);
        }
 
        (success,) = address(marketingWallet).call{value: address(this).balance}("");
    }
}